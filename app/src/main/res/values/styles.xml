<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:textColorHint">@color/hintTextColor</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SeachView" parent="Widget.AppCompat.EditText">
        <item name="android:textColorHint">@color/searchViewUnfocusedTextColor</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:background">@null</item>
    </style>

    <style name="TitleText">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">35sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ButtonBase" parent="TextAppearance.AppCompat.Widget.Button">
        <item name="android:padding">15dp</item>>
        <item name="android:background">@drawable/button_background</item>>
        <item name="android:textAllCaps">false</item>>
        <item name="android:textColor">@android:color/white</item>>
        <item name="android:textSize">20sp</item>>
    </style>

    <style name="EditTextBase" parent="Widget.AppCompat.EditText">
        <item name="android:backgroundTint">@color/hintTextColor</item>
    </style>

</resources>
